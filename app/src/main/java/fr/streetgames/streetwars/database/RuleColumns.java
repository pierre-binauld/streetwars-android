package fr.streetgames.streetwars.database;

public interface RuleColumns {

    String ID = Tables.RULE + "_id";

    String GROUP = Tables.RULE + "_group";

    String RULE = Tables.RULE + "_fule";
}
